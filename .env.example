# Never commit a real key. Set this value only in your local shell or .env file.
OPENROUTER_API_KEY=

# Safety guard for paid runs. The runner refuses a batch whose estimate is above this value.
EVAL_MAX_BATCH_COST_USD=3.00
